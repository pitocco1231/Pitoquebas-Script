-- 🔐 SISTEMA DE KEY (API) + FIXES

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

print("Script iniciou")

local API = "https://pitoquebas-script-1.onrender.com/check"

-- request universal
local function getRequest()
    return (syn and syn.request)
        or (http and http.request)
        or http_request
        or (fluxus and fluxus.request)
        or request
end

local function CheckKey(key)
    local request = getRequest()

    if not request then
        warn("Executor não suporta request")
        return false
    end

    local url = API .. "?key=" .. key .. "&user=" .. LocalPlayer.UserId

    local success, res = pcall(function()
        return request({
            Url = url,
            Method = "GET"
        })
    end)

    if success and res and res.StatusCode == 200 then
        local data = HttpService:JSONDecode(res.Body)
        return data.valid
    else
        warn("Erro ao verificar key")
    end

    return false
end

-- 🖥️ GUI (FUNCIONA EM TODOS)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PitoLogin"

pcall(function()
    ScreenGui.Parent = game:GetService("CoreGui")
end)

if not ScreenGui.Parent then
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
end

local Frame = Instance.new("Frame", ScreenGui)
Frame.Size = UDim2.new(0, 300, 0, 160)
Frame.Position = UDim2.new(0.5, -150, 0.5, -80)
Frame.BackgroundColor3 = Color3.fromRGB(35,35,35)

local TextBox = Instance.new("TextBox", Frame)
TextBox.Size = UDim2.new(0, 240, 0, 35)
TextBox.Position = UDim2.new(0.5, -120, 0.4, 0)
TextBox.PlaceholderText = "Insira sua Key..."
TextBox.Text = ""

local Btn = Instance.new("TextButton", Frame)
Btn.Size = UDim2.new(0, 150, 0, 35)
Btn.Position = UDim2.new(0.5, -75, 0.75, 0)
Btn.Text = "VERIFICAR"

local Liberado = false

Btn.MouseButton1Click:Connect(function()
    print("Botão clicado")

    Btn.Text = "Verificando..."

    local ok = false

    local success = pcall(function()
        ok = CheckKey(TextBox.Text)
    end)

    if success and ok then
        Btn.Text = "Sucesso!"
        Liberado = true
        task.wait(1)
        ScreenGui:Destroy()
    else
        Btn.Text = "Key inválida!"
        task.wait(1.5)
        Btn.Text = "VERIFICAR"
    end
end)

-- trava até logar
repeat task.wait() until Liberado

print("Login passou")

-- 🔥 SCRIPT PRINCIPAL

-- Anti-Kick
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    if method == "Kick" or method == "kick" then return nil end
    return oldNamecall(self, ...)
end)
setreadonly(mt, true)

-- UI
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "Pitoquebas Scripts | V4",
    LoadingTitle = "Iniciando Sistema...",
    LoadingSubtitle = "por Pitoquebas",
    ConfigurationSaving = {Enabled = false},
    KeySystem = false,
    Theme = "Default"
})

-- Config
local Settings = {
    AutoToken = false,
    AutoAbility = false,
    AutoYen = false
}

-- Remote
local RewardRemote
pcall(function()
    RewardRemote = game:GetService("ReplicatedStorage")
        .Packages._Index["sleitnick_knit@1.7.0"]
        .knit.Services.SeasonService.RF.RequestRankedReward
end)

-- Loop
task.spawn(function()
    while true do
        if RewardRemote then
            if Settings.AutoToken then 
                pcall(function() RewardRemote:InvokeServer(1) end)
                task.wait(math.random(3,4))
            end
            if Settings.AutoAbility then 
                pcall(function() RewardRemote:InvokeServer(4) end)
                task.wait(math.random(3,4))
            end
            if Settings.AutoYen then 
                pcall(function() RewardRemote:InvokeServer(2) end)
                task.wait(math.random(3,4))
            end
        end
        task.wait(0.5)
    end
end)

-- Tabs
local MainTab = Window:CreateTab("Auto Farm", 4483362458)

MainTab:CreateToggle({
    Name = "Auto Token 🔄",
    CurrentValue = false,
    Callback = function(v) Settings.AutoToken = v end
})

MainTab:CreateToggle({
    Name = "Auto Ability ✨",
    CurrentValue = false,
    Callback = function(v) Settings.AutoAbility = v end
})

MainTab:CreateToggle({
    Name = "Auto Yen 💰",
    CurrentValue = false,
    Callback = function(v) Settings.AutoYen = v end
})

Rayfield:Notify({
    Title = "Pitoquebas Scripts",
    Content = "Script carregado com sucesso!",
    Duration = 5
})
